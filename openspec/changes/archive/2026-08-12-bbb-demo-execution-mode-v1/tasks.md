# Tasks

- [x] Add ABI-only Demo Compose override.
- [x] Document `BBB_DATA_ROOT` and `BBB_SECRETS_ROOT` local layout.
- [x] Record the Demo override contract in OpenSpec.
- [x] Verify base Compose config remains safe and does not require secrets.
- [x] Verify Demo Compose config requires `BBB_SECRETS_ROOT` and overrides only ABI.
